# Readme

Clone this repo in your home dir, e.g., in `~/dotfiles` and `cd ~/dotfiles`. Then run `stow .` to automatically make symlinks out of the existing files in this repo. `.gitignore` will be automatically stored by the default stow ignore file.

# For a different target do
stow --target=~ .

